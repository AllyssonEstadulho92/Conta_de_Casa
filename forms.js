function openDialog(title, html) { $('#dialogTitle').textContent=title; $('#dialogBody').innerHTML=html; $('#formDialog').showModal(); }
function closeDialog(){ $('#formDialog').close(); }
function billFormHtml(bill=null){
  const due=bill?new Date(bill.dueAt):new Date(); if(!bill) due.setDate(due.getDate()+7);
  const date=due.toISOString().slice(0,10); const time=`${String(due.getHours()).padStart(2,'0')}:${String(due.getMinutes()).padStart(2,'0')}`;
  return `<form id="billForm" class="form-grid two"><input type="hidden" name="id" value="${bill?.id||''}"><label>Descrição<input name="title" required maxlength="80" value="${esc(bill?.title||'')}"></label><label>Fornecedor/entidade<input name="provider" maxlength="80" value="${esc(bill?.provider||'')}"></label><label>Categoria<input name="category" list="categoryList" required value="${esc(bill?.category||'Casa')}"><datalist id="categoryList"><option>Casa</option><option>Energia</option><option>Água</option><option>Internet</option><option>Saúde</option><option>Educação</option><option>Transporte</option><option>Seguros</option><option>Outros</option></datalist></label><label>Valor total<input name="amount" inputmode="decimal" required value="${bill?(bill.totalCents/100).toFixed(2).replace('.',','):''}" placeholder="0,00"></label><label>Vencimento<input name="dueDate" type="date" required value="${date}"></label><label>Hora limite<input name="dueTime" type="time" value="${time||'23:59'}"></label><label>Método<select name="method"><option>Débito automático</option><option>Transferência</option><option>Referência Multibanco</option><option>Cartão</option><option>Dinheiro</option><option>Outro</option></select></label><label>Recorrência<select name="recurrence"><option value="none">Sem recorrência</option><option value="weekly">Semanal</option><option value="monthly">Mensal</option><option value="quarterly">Trimestral</option><option value="semiannual">Semestral</option><option value="annual">Anual</option></select></label><label class="full-row">Referência<input name="reference" value="${esc(bill?.reference||'')}"></label><label class="full-row">Observações<textarea name="notes">${esc(bill?.notes||'')}</textarea></label><div class="button-row full-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button type="submit" class="btn primary">${bill?'Guardar alterações':'Criar fatura'}</button></div></form>`;
}
function openBillForm(bill=null){ openDialog(bill?'Editar fatura':'Nova fatura',billFormHtml(bill)); const f=$('#billForm'); if(bill){f.method.value=bill.method||'Outro';f.recurrence.value=bill.recurrence||'none';} f.addEventListener('submit',handleBillSubmit); }
async function handleBillSubmit(e){ e.preventDefault(); const fd=new FormData(e.currentTarget); const total=parseCents(fd.get('amount')); if(!Number.isFinite(total)||total<=0){toast('Introduza um valor superior a zero.');return;} const due=new Date(`${fd.get('dueDate')}T${fd.get('dueTime')||'23:59'}`); if(Number.isNaN(due.getTime())){toast('Data de vencimento inválida.');return;} const data={title:String(fd.get('title')).trim(),provider:String(fd.get('provider')).trim(),category:String(fd.get('category')).trim()||'Outros',totalCents:total,dueAt:due.toISOString(),method:String(fd.get('method')),recurrence:String(fd.get('recurrence')),reference:String(fd.get('reference')).trim(),notes:String(fd.get('notes')).trim(),updatedAt:new Date().toISOString()}; const id=String(fd.get('id')||''); if(id){const b=appState.bills.find(x=>x.id===id);Object.assign(b,data);await commit(`Fatura “${data.title}” atualizada`,'bill');}else{appState.bills.push({id:uid(),...data,createdAt:new Date().toISOString(),cancelled:false,archived:false});await commit(`Fatura “${data.title}” criada`,'bill');} closeDialog(); toast('Fatura guardada.'); }

function openBillDetail(id){ const b=appState.bills.find(x=>x.id===id); if(!b)return; const paid=paidForBill(id),rem=remainingForBill(b),st=billStatus(b),urg=billUrgency(b); const payments=billPayments(id).sort((a,c)=>new Date(c.paidAt)-new Date(a.paidAt)); openDialog('Detalhes da fatura',`<div class="detail-grid"><div class="detail-item"><small>Descrição</small><strong>${esc(b.title)}</strong></div><div class="detail-item"><small>Estado</small><strong>${statusLabel(st)}</strong></div><div class="detail-item"><small>Valor total</small><strong data-money>${money(b.totalCents)}</strong></div><div class="detail-item"><small>Restante</small><strong data-money>${money(rem)}</strong></div><div class="detail-item"><small>Vencimento</small><strong>${fmtDateTime(b.dueAt)}</strong></div><div class="detail-item"><small>Urgência</small><strong>${urgencyLabel(urg)}</strong></div><div class="detail-item"><small>Fornecedor</small><strong>${esc(b.provider||'—')}</strong></div><div class="detail-item"><small>Categoria</small><strong>${esc(b.category||'Outros')}</strong></div></div><h3 style="margin-top:18px">Pagamentos</h3><div class="stack-list">${payments.length?payments.map(p=>`<div class="list-row"><div class="list-main"><strong data-money>${money(p.amountCents)}</strong><small>${fmtDateTime(p.paidAt)} · ${esc(p.method||'')}</small></div></div>`).join(''):empty('Sem pagamentos registados.')}</div><div class="button-row" style="margin-top:18px">${rem>0&&!b.cancelled?`<button class="btn primary" data-detail-pay="${b.id}">Registar pagamento</button>`:''}<button class="btn secondary" data-detail-edit="${b.id}">Editar</button>${!b.cancelled?`<button class="btn danger" data-detail-cancel="${b.id}">Cancelar fatura</button>`:''}</div>`); }
function openPaymentForm(id){ const b=appState.bills.find(x=>x.id===id); if(!b)return; const rem=remainingForBill(b); openDialog('Registar pagamento',`<form id="paymentForm" class="form-grid"><input type="hidden" name="billId" value="${b.id}"><p>Fatura: <strong>${esc(b.title)}</strong><br>Restante: <strong data-money>${money(rem)}</strong></p><label>Valor<input name="amount" inputmode="decimal" required value="${(rem/100).toFixed(2).replace('.',',')}"></label><label>Data e hora<input name="paidAt" type="datetime-local" required value="${localDateTimeInput()}"></label><label>Método<select name="method"><option>Débito automático</option><option>Transferência</option><option>Referência Multibanco</option><option>Cartão</option><option>Dinheiro</option><option>Outro</option></select></label><label>Nota<input name="notes"></label><div class="button-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button class="btn primary" type="submit">Guardar pagamento</button></div></form>`); $('#paymentForm').addEventListener('submit',handlePaymentSubmit); }
async function handlePaymentSubmit(e){e.preventDefault();const fd=new FormData(e.currentTarget);const b=appState.bills.find(x=>x.id===fd.get('billId'));if(!b)return;const amount=parseCents(fd.get('amount'));const rem=remainingForBill(b);if(!Number.isFinite(amount)||amount<=0||amount>rem){toast(`O pagamento deve estar entre 0,01 e ${money(rem)}.`);return;}const at=new Date(fd.get('paidAt'));if(Number.isNaN(at.getTime())){toast('Data de pagamento inválida.');return;}appState.payments.push({id:uid(),billId:b.id,amountCents:amount,paidAt:at.toISOString(),method:String(fd.get('method')),notes:String(fd.get('notes')).trim(),createdAt:new Date().toISOString()});await syncRecurringBills();await commit(`Pagamento de ${money(amount)} registado em “${b.title}”`,'payment');closeDialog();toast('Pagamento registado.');}

function openIncomeForm(){openDialog('Novo rendimento',`<form id="incomeForm" class="form-grid"><label>Descrição<input name="description" required placeholder="Ex.: Salário"></label><label>Valor<input name="amount" inputmode="decimal" required placeholder="0,00"></label><label>Data<input name="receivedAt" type="datetime-local" required value="${localDateTimeInput()}"></label><div class="button-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button class="btn primary">Guardar</button></div></form>`);$('#incomeForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const amount=parseCents(fd.get('amount'));if(!Number.isFinite(amount)||amount<=0){toast('Valor inválido.');return;}appState.incomes.push({id:uid(),description:String(fd.get('description')).trim(),amountCents:amount,receivedAt:new Date(fd.get('receivedAt')).toISOString(),createdAt:new Date().toISOString()});await commit(`Rendimento “${fd.get('description')}” registado`,'income');closeDialog();toast('Rendimento guardado.');});}
function openMarketForm(){openDialog('Adicionar ao mercado',`<form id="marketForm" class="form-grid two"><label>Produto<input name="name" required></label><label>Categoria<input name="category" placeholder="Frutas, limpeza..."></label><label>Quantidade<input name="quantity" value="1"></label><label>Unidade<select name="unit"><option>un</option><option>kg</option><option>g</option><option>L</option><option>ml</option></select></label><label class="full-row">Preço estimado<input name="estimated" inputmode="decimal" placeholder="0,00"></label><div class="button-row full-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button class="btn primary">Adicionar</button></div></form>`);$('#marketForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),est=parseCents(fd.get('estimated'));appState.market.push({id:uid(),name:String(fd.get('name')).trim(),category:String(fd.get('category')).trim(),quantity:String(fd.get('quantity')).trim()||'1',unit:String(fd.get('unit')),estimatedCents:Number.isFinite(est)?est:0,actualCents:0,purchased:false,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),purchasedAt:null});await commit(`Item “${fd.get('name')}” adicionado ao mercado`,'market');closeDialog();toast('Item adicionado.');});}
function openGoalForm(){openDialog('Novo objetivo',`<form id="goalForm" class="form-grid"><label>Objetivo<input name="name" required placeholder="Ex.: Fundo de emergência"></label><label>Meta<input name="target" inputmode="decimal" required placeholder="0,00"></label><label>Já poupado<input name="saved" inputmode="decimal" value="0,00"></label><label>Prazo opcional<input name="deadline" type="date"></label><div class="button-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button class="btn primary">Criar objetivo</button></div></form>`);$('#goalForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),target=parseCents(fd.get('target')),saved=parseCents(fd.get('saved'));if(!Number.isFinite(target)||target<=0||!Number.isFinite(saved)||saved<0){toast('Valores inválidos.');return;}appState.goals.push({id:uid(),name:String(fd.get('name')).trim(),targetCents:target,savedCents:Math.min(saved,target),deadline:fd.get('deadline')?new Date(`${fd.get('deadline')}T12:00:00`).toISOString():null,createdAt:new Date().toISOString(),archived:false});await commit(`Objetivo “${fd.get('name')}” criado`,'goal');closeDialog();toast('Objetivo criado.');});}
function openGoalContribution(id){const g=appState.goals.find(x=>x.id===id);if(!g)return;openDialog('Adicionar ao objetivo',`<form id="goalAddForm" class="form-grid"><p><strong>${esc(g.name)}</strong><br>Falta <span data-money>${money(Math.max(0,g.targetCents-g.savedCents))}</span></p><label>Valor<input name="amount" inputmode="decimal" required placeholder="0,00"></label><div class="button-row"><button type="button" class="btn secondary" data-close-dialog>Cancelar</button><button class="btn primary">Adicionar</button></div></form>`);$('#goalAddForm').addEventListener('submit',async e=>{e.preventDefault();const amount=parseCents(new FormData(e.currentTarget).get('amount'));if(!Number.isFinite(amount)||amount<=0){toast('Valor inválido.');return;}g.savedCents=Math.min(g.targetCents,g.savedCents+amount);await commit(`Valor adicionado ao objetivo “${g.name}”`,'goal');closeDialog();toast('Objetivo atualizado.');});}
function openMoreMenu(){openDialog('Mais secções',`<div class="quick-grid">${[['calendar','Calendário','calendar'],['market','Mercado','market'],['reports','Relatórios','report'],['goals','Objetivos','goal'],['security','Segurança','shield'],['settings','Configurações','settings']].map(([id,label,ic])=>`<button type="button" data-more-page="${id}">${icon(ic,22)}<br>${label}</button>`).join('')}</div>`);}

async function exportBackup(){const meta=await idbGet('meta','vault'),secure=await idbGet('secure','state');const blob=new Blob([JSON.stringify({app:'Conta_de_Casa',formatVersion:1,exportedAt:new Date().toISOString(),meta,secure},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`Conta_de_Casa_backup_${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);$('#backupMessage').textContent='Backup cifrado exportado.';}
async function importBackup(file){const obj=JSON.parse(await file.text());if(obj?.app!=='Conta_de_Casa'||!obj.meta?.salt||!obj.secure?.cipher)throw new Error('Ficheiro de backup inválido.');await idbPut('meta',{key:'vault',...obj.meta});await idbPut('secure',{key:'state',...obj.secure});$('#backupMessage').textContent='Backup restaurado. Desbloqueie com a palavra-passe do backup.';lockApp();}
function lockApp(){vaultKey=null;appState=null;$('#app').hidden=true;$('#vaultScreen').hidden=false;$('#vaultCreate').hidden=true;$('#vaultUnlock').hidden=false;$('#unlockPassphrase').value='';$('#unlockPassphrase').focus();}
function resetLockTimer(){clearTimeout(lockTimer);if(!vaultKey||!appState)return;const mins=Number(appState.settings.lockMinutes)||15;lockTimer=setTimeout(()=>{if(vaultKey)lockApp();},mins*60000);}
