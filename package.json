{
  "name": "conta-de-casa",
  "private": true,
  "version": "0.76.0",
  "description": "Conta de Casa — PWA local-first para gestão financeira doméstica, Mercado e planeamento.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build:runtime": "node scripts/build-typescript-runtime.cjs",
    "build:pages": "node scripts/prepare-pages.cjs",
    "test:e2e": "playwright test",
    "serve:dist": "node scripts/serve-dist.cjs"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "@playwright/test": "1.63.0",
    "@zxing/browser": "0.2.0"
  }
}
